SELECT PAR.IP_ID AS IP_ID, PI.IP AS IP, COUNT(PAR.IP_ID) AS COUNTER FROM PARSER_ACCESS_REGISTRY AS PAR
INNER JOIN PARSER_IP AS PI ON (PAR.IP_ID = PI.ID)
WHERE PAR.ACCESS_DATE BETWEEN '2017-01-01 13:00:00' AND '2017-01-01 14:00:00' GROUP BY PAR.IP_ID HAVING COUNT(PAR.IP_ID) >= 100;